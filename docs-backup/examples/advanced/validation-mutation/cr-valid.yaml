# test/cr-valid.yaml
# Test case E: SHOULD SUCCEED — all rules pass, no warnings
# Expected: created with no errors, no warnings
# Deployment and Service created by reconciler
apiVersion: demo.orkestra.io/v1alpha1
kind: Website
metadata:
  name: valid-site
  namespace: orkestra
  labels:
    team: platform          # satisfies the warn rule
spec:
  image: myorg/nginx:1.25  # satisfies the deny rule
  replicas: 3               # satisfies min:1 and max:10
  port: 8080
  logLevel: debug           # user-declared — mutation default is not applied
  enabled: true             # satisfies the warn rule
