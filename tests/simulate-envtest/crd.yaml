apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: probes.integration.orkestra.io
spec:
  group: integration.orkestra.io
  names:
    kind: Probe
    listKind: ProbeList
    plural: probes
    singular: probe
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                image:
                  type: string
                replicas:
                  type: integer
            status:
              type: object
              # x-kubernetes-preserve-unknown-fields allows the reconciler to write
              # standard status fields (conditions, observedGeneration, etc.) that are
              # not explicitly declared in the schema, without triggering API server
              # warnings or validation rejections.
              x-kubernetes-preserve-unknown-fields: true
              properties:
                phase:
                  type: string
                message:
                  type: string
