apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: conditional-reconciliation-envtest
  description: >
    Suite for the kordinator pre-reconcile gate.
    Two scenarios: gate passes (Deployment created) and gate discards (no Deployment).

imports:
  - ./simulate-gate-pass.yaml
  - ./simulate-gate-discard.yaml
