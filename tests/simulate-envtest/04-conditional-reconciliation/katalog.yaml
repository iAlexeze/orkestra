apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: gate-operator
  version: 0.1.0
  description: >
    Gate operator used in envtest simulate tests.
    Verifies the kordinator pre-reconcile gate: when spec.enabled is false,
    the reconciler is never called and no Deployment is created.

spec:
  crds:
    probe:
      crdFile: ./crd.yaml

      operatorBox:
        preReconcile:
          reconcileGate:
            when:
              - field: "{{ .spec.enabled }}"
                equals: "true"

        status:
          fields:
            - path: phase
              value: "Running"

        onReconcile:
          deployments:
            - name: "{{ .metadata.name }}"
              image: "{{ .spec.image }}"
              replicas: "{{ .spec.replicas }}"
              reconcile: true
