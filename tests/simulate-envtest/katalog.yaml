apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: probe-operator
  version: 0.1.0
  description: Probe operator used in envtest simulate tests.

spec:
  crds:
    probe:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml
      allowedNamespaces:
        - default

      operatorBox:
        status:
          fields:
            - path: phase
              value: "Running"
            - path: message
              value: "reconciled"

        onReconcile:
          deployments:
            - name: "{{ .metadata.name }}"
              image: "{{ .spec.image }}"
              replicas: "{{ .spec.replicas }}"
              reconcile: true
