apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: informer-probe-simulate
  description: >
    Runs both namespace scenarios: CR in allowed namespace (team-a) produces
    a ConfigMap; CR in restricted namespace (team-b) produces nothing.

imports:
  - simulate/approved.yaml
  - simulate/restricted.yaml
