apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: informer-probe
  author: orkspace
  version: 0.1.0
  description: >
    Living fixture for pkg/informer. A minimal declarative operator used to
    verify that the informer factory correctly watches a CRD, syncs the cache,
    and triggers reconciles. e2e assertions use /katalog/informer-probe/health
    and /katalog/informer-probe/cr via leader port-forward.

spec:
  crds:
    informer-probe:
      apiTypes:
        group: informer.orkestra.io
        version: v1alpha1
        kind: InformerProbe
        plural: informerprobes

      allowedNamespaces:
        - team-a

      operatorBox:
        reconciler:
          workers: 2
          resync: 10s
        onCreate:
          configMaps:
            - name: "{{ .metadata.name }}-config"
              namespace: "{{ .metadata.namespace }}"
              data:
                observed: "{{ .metadata.name }}"
        status:
          fields:
            - path: phase
              value: Running
            - path: observed
              value: "{{ .metadata.name }}"
