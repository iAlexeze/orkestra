expect:
  # All 3 CRDs were in the missing map at startup — retryMissingCRDs activates
  # them together (Phase 2 of the retry loop, no dependency check). All become
  # pending with workers running. timeout covers the 90s retry interval.
  - name: CRDs installed — reconcilers started, all pending
    after: cr-applied
    timeout: 2m
    kubectl:
      apply:
        - file: ./crd-worker.yaml
        - file: ./crd-component.yaml
        - file: ./crd-platform.yaml
      port-forward:
        - leaderElection:
            lease: orkestra-konductor
            namespace: orkestra-system
          port: 8080
          path: /katalog/worker/health
          jq: .state
          equals: pending
        - leaderElection:
            lease: orkestra-konductor
            namespace: orkestra-system
          port: 8080
          path: /katalog/worker/health
          jq: .started
          equals: "true"
        - leaderElection:
            lease: orkestra-konductor
            namespace: orkestra-system
          port: 8080
          path: /katalog/component/health
          jq: .state
          equals: pending
        - leaderElection:
            lease: orkestra-konductor
            namespace: orkestra-system
          port: 8080
          path: /katalog/component/health
          jq: .started
          equals: "true"
        - leaderElection:
            lease: orkestra-konductor
            namespace: orkestra-system
          port: 8080
          path: /katalog/platform/health
          jq: .state
          equals: pending
