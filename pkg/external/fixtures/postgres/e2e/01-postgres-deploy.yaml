expect:
  - name: deploy PostgreSQL in orkestra-system
    kubectl:
      apply:
        - inline: |
            apiVersion: apps/v1
            kind: Deployment
            metadata:
              name: postgres
              namespace: orkestra-system
            spec:
              replicas: 1
              selector:
                matchLabels:
                  app: postgres
              template:
                metadata:
                  labels:
                    app: postgres
                spec:
                  containers:
                    - name: postgres
                      image: postgres:16-alpine
                      env:
                        - name: POSTGRES_USER
                          value: orkestra
                        - name: POSTGRES_PASSWORD
                          value: orkestra
                        - name: POSTGRES_DB
                          value: orkestra
                      ports:
                        - containerPort: 5432
        - inline: |
            apiVersion: v1
            kind: Service
            metadata:
              name: postgres
              namespace: orkestra-system
            spec:
              selector:
                app: postgres
              ports:
                - port: 5432
                  targetPort: 5432

  - name: PostgreSQL pod is ready
    timeout: 90s
    resources:
      - kind: Deployment
        name: postgres
        namespace: orkestra-system
        condition: Available
