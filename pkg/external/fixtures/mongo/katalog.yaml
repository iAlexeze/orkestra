apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: external-mongo
  description: >
    Verifies protocol: mongo using a MongoDB instance deployed in orkestra-system.
    Queries the orkestra.test collection to assert connectivity and document count.

spec:
  crds:
    webapp:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml

      operatorBox:
        reconciler:
          resync: 20s

        onReconcile:
          external:
            - name: docs
              protocol: mongo
              url: "{{ .spec.mongoURL }}"
              query: "orkestra.test"
              continueOnError: true
              timeout: 5s

            - name: activeConnections
              protocol: mongo
              url: "{{ .spec.mongoURL }}"
              query: 'orkestra.test {"status": "active"}'
              continueOnError: true
              timeout: 5s
              cacheFor: 10s

          deployments:
            - name: "{{ .metadata.name }}"
              image: "{{ .spec.image }}"
              replicas: 1

        status:
          fields:
            - path: docCount
              value: "{{ .external.docs.count }}"

            - path: activeCount
              value: "{{ .external.activeConnections.count }}"

            - path: phase
              value: "Ready"
              when:
                - field: external.docs.error
                  operator: notExists

            - path: phase
              value: "Degraded"
              when:
                - field: external.docs.error
                  operator: exists
