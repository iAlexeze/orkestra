apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: requeue-after-template
  version: 0.1.0
  description: Valid requeue.after with a template expression.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        reconciler:
          requeue:
            after: '{{ .spec.checkInterval | default "60s" }}'
            when:
              - field: spec.enabled
                equals: "true"
