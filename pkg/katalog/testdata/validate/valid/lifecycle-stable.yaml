apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: stable-operator
  version: 2.0.0
  description: Tests a full lifecycle block on a stable Katalog.
lifecycle:
  maturity: stable
  compatibility:
    kubernetes: ">=1.28"
    orkestra: ">=0.7.0"
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
