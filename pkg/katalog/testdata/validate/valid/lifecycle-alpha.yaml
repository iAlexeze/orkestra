apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: alpha-operator
  version: 0.1.0
  description: Tests that alpha maturity passes validation (warns but does not error).
lifecycle:
  maturity: alpha
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
