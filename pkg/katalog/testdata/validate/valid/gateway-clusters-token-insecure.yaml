apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: gateway-clusters-token-insecure
  version: 1.0.0
  description: Valid gateway.clusters — bearer token with insecure:true, no caRef required.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
gateway:
  enabled: true
  clusters:
    dev:
      endpoint: https://dev.internal:6443
      tokenRef:
        name: dev-sa-token
        key: token
      insecure: true
