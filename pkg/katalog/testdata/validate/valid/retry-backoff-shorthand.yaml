apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: retry-backoff-shorthand
  version: 0.1.0
  description: Valid retryBackoff shorthand on queue and external.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        reconciler:
          resync: 10m
          queue:
            retryBackoff: 5s
