apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: legacy-worker
  version: 1.0.0
  description: "Tests deprecation past end-of-life date — validates cleanly at schema level."
lifecycle:
  deprecation:
    message: "Replaced by task-runner which uses the Jobs API"
    migratedTo: task-runner:v1.0.0
    timeline:
      from: "2020-01-01"
      to:   "2021-01-01"
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
