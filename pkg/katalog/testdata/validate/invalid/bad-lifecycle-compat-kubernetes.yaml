apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-compat-kubernetes
  version: 1.0.0
  description: Tests that an invalid kubernetes semver range fails validation.
lifecycle:
  compatibility:
    kubernetes: "!!!invalid"
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
