apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-compat-orkestra
  version: 1.0.0
  description: Tests that an invalid orkestra semver range fails validation.
lifecycle:
  compatibility:
    orkestra: "not-a-range"
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
