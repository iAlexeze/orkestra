apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-serve-target-mode
  version: 1.0.0
  description: Tests that serve.modes where serve is enabled but all modes are disabled fail validation.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      serve:
        enabled: true
        namespace: default
        modes:
          target: false
          cr: false