# Triggers: enrichAll and enrich are mutually exclusive
apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-enrich-exclusive
  version: 0.1.0
  description: Tests that enrichAll and enrich are mutually exclusive.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      enrichAll: true
      enrich:
        - pods
