apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-serve-target-mode
  version: 1.0.0
  description: Tests that serve.modes where serve.modes.target is disabled but serve.target is enabled fail validation.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      serve:
        enabled: true
        namespace: default
        modes:
          target: false
        target:
          primary:
            primary: true
