apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: publish-intent-no-gateway
  version: 1.0.0
  description: Tests that intent true requires gateway.api.enabled.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
publish:
  tests:
    intent: true
