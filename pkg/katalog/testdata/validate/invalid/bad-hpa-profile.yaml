# Triggers: unknown HPA behavior.profile
apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-hpa-profile
  version: 0.1.0
  description: Tests that an unknown HPA behavior.profile is rejected.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        onCreate:
          hpa:
            - name: widget-hpa
              minReplicas: 1
              maxReplicas: 10
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: widget-controller
              behavior:
                profile: aggressive
