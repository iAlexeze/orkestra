apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-requeue-after
  version: 0.1.0
  description: Invalid requeue.after — not a duration or template.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        reconciler:
          requeue:
            after: "not-a-duration"
