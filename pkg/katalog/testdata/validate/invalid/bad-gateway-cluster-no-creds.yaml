apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-gateway-cluster-no-creds
  version: 1.0.0
  description: Tests that a cluster entry without any credential form fails validation.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
gateway:
  enabled: true
  clusters:
    prod:
      endpoint: https://prod.internal:6443
