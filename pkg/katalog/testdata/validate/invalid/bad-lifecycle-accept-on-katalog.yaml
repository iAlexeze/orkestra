apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-accept-on-katalog
  version: 1.0.0
  description: Tests that lifecycle.accept is rejected on a Katalog (Komposer-only field).
lifecycle:
  accept:
    patterns:
      - name: some-deprecated-import
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
