apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-retry-backoff-negative-multiplier
  version: 0.1.0
  description: Invalid retryBackoff — negative multiplier.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        reconciler:
          queue:
            retryBackoff:
              initial: 500ms
              multiplier: -1.0
