apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: bad-retry-backoff-max-lt-initial
  version: 0.1.0
  description: Invalid retryBackoff — max less than initial.
spec:
  crds:
    widget:
      crdFile: ../crd.yaml
      operatorBox:
        reconciler:
          queue:
            retryBackoff:
              initial: 30s
              max: 1s
