apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: test
gateway:
  enabled: true
  api:
    enabled: true
    auth:
      tokens:
        - name: dev
          token: "${ORK_DEV_TOKEN}"
spec:
  crds:
    platform:
      apiTypes:
        group: platform.orkestra.io
        version: v1alpha1
        kind: Platform
        plural: platforms
      serve:
        enabled: true
        fields:
          name:
            label: "Name"
            order: 1
