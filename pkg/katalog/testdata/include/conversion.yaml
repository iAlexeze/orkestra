apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: test
spec:
  crds:
    platform:
      apiTypes:
        group: platform.orkestra.io
        version: v1
        kind: Platform
        plural: platforms
      conversion:
        storageVersion: v1
        include: ./conversion-paths.yaml
        paths:
          - from: v1
            to: v1alpha1
            spec:
              image: "{{ .spec.image }}"
