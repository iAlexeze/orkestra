apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: test
gateway:
  applyAPI:
    enabled: true
    auth:
      tokens:
        - name: cc
          secretRef:
            name: ork-apply-token
            key: token
spec:
  crds:
    platform:
      apiTypes:
        group: platform.orkestra.io
        version: v1alpha1
        kind: Platform
        plural: platforms
      idp:
        enabled: true
        include: ./idp-fields.yaml
        fields:
          image:
            label: "Image"
            order: 3
