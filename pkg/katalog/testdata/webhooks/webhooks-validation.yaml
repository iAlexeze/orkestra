apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: test
spec:
  crds:
    platform:
      apiTypes:
        group: platform.orkestra.io
        version: v1alpha1
        kind: Platform
        plural: platforms
      validation:
        rules:
          - field: spec.name
            operator: exists
            message: "spec.name is required"
            action: deny
security:
  webhooks:
    admission:
      enabled: true
