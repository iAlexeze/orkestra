apiVersion: orkestra.orkspace.io/v1
kind: Motif
metadata:
  version: v1

inputs:
  - name: image
    required: true

resources:
  deployments:
    - name: app
      image: "{{ index .inputs \"image\" }}"
