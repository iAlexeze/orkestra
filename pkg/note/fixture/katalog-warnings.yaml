apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: warnings-probe
  author: orkspace
  version: 0.1.0
  description: >
    WarningsProbe — integration test for warning event enrichment.
    Creates a Deployment that will generate warning events (deliberately
    misconfigured image) with enrich: [events].
    Apply the CR and inspect: kubectl get warningsprobe <name> -o yaml
    Reuses the NoteProbe CRD.

spec:
  crds:
    warnings-probe:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml

      enrich:
        - events

      operatorBox:
        reconciler:
          workers: 1
          resync: 30s

        onCreate:
          deployments:
            - name: "{{ .metadata.name }}-app"
              image: "{{ .spec.image }}"
              replicas: "{{ .spec.replicas }}"
              port: "{{ .spec.port }}"
              reconcile: true

        status:
          fields:
            # ── Phase state machine ─────────────────────────────────────────
            - path: phase
              value: "Pending"
              when:
                - field: status.phase
                  operator: notExists

            - path: phase
              value: "Degraded"
              when:
                - field: "{{ hasWarnings .children.deployment }}"
                  equals: "true"
                - field: "{{ allReplicasReady .children.deployment }}"
                  equals: "false"

            - path: phase
              value: "Ready"
              when:
                - field: "{{ allReplicasReady .children.deployment }}"
                  equals: "true"

            # ── replica notes ───────────────────────────────────────────────
            - path: allReady
              value: "{{ allReplicasReady .children.deployment }}"

            # ── warning event notes (require enrich: [events]) ──────────────
            - path: hasWarnings
              value: "{{ hasWarnings .children.deployment }}"

            - path: warningCount
              value: "{{ warningCount .children.deployment }}"

            - path: firstWarning
              value: "{{ firstWarning .children.deployment }}"

            - path: warningMessages
              value: "{{ warningMessages .children.deployment }}"

            - path: warningReasons
              value: "{{ warningReasons .children.deployment }}"
