apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: pod-node-probe
  author: orkspace
  version: 0.1.0
  description: >
    PodNodeProbe — living integration test for pod node enrichment notes.
    Declares a standalone Pod (not via a Deployment) with enrich: [node]
    so that _node is embedded after the pod is scheduled.
    Apply the CR and inspect: kubectl get podnodeprobe <name> -o yaml
    Reuses the NoteProbe CRD.

spec:
  crds:
    pod-node-probe:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml

      workers: 1
      resync: 30s

      enrich:
        - node

      operatorBox:
        onCreate:
          pods:
            - name: "{{ .metadata.name }}-pod"
              image: "{{ .spec.image }}"
              reconcile: true

        status:
          fields:
            # ── Phase ───────────────────────────────────────────────────────
            - path: phase
              value: "Pending"
              when:
                - field: status.phase
                  operator: notExists

            - path: phase
              value: "Running"
              when:
                - field: "{{ podNodeName .children.pod }}"
                  operator: exists

            # ── enriched pod node notes (require enrich: [node]) ────────────
            - path: nodeName
              value: "{{ podNodeName .children.pod }}"

            - path: nodeZone
              value: "{{ podNodeZone .children.pod }}"

            - path: nodeRegion
              value: "{{ podNodeRegion .children.pod }}"

            - path: nodeInstanceType
              value: "{{ podNodeInstanceType .children.pod }}"
