# Happy path monitoring workload — all required fields present, no rules fire.
# The deny anyOf (repoURL) is satisfied; no production warn since environment=staging.
apiVersion: gateway.fixture.orkestra.io/v1alpha1
kind: PlatformResource
metadata:
  name: platform-monitor
  namespace: team-platform
spec:
  workloadType: monitoring
  team: team-platform
  environment: staging
  repoURL: https://github.com/myorg/platform
  scrapeInterval: 30s
