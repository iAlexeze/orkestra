apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: gateway-idp-fixture-sim
  description: >
    Gateway IDP fixture — aggregates all three workload type simulates.
    Run individually via simulate/<workload>.yaml for focused iteration.

imports:
  - ./simulate/app.yaml
  - ./simulate/cert.yaml
  - ./simulate/monitoring.yaml
