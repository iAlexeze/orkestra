# Exercises: ClusterPolicy (serve disabled) — NetworkPolicy created on reconcile.
apiVersion: gateway.fixture.orkestra.io/v1alpha1
kind: ClusterPolicy
metadata:
  name: platform-egress
spec:
  targetNamespace: team-payments
