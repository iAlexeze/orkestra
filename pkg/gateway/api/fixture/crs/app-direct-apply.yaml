# Direct apply: no provenance annotations — simulates a kubectl apply bypassing the Gateway API.
# Exercises: warn when: isDirectApply . → should fire ValidationWarning.
apiVersion: gateway.fixture.orkestra.io/v1alpha1
kind: PlatformResource
metadata:
  name: payments-api-direct
  namespace: team-payments
spec:
  workloadType: app
  team: team-payments
  environment: staging
  repoURL: https://github.com/myorg/payments-api
  targetRevision: main
  targetNamespace: team-payments
