apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: gateway-fixture-cert
  description: cert workload — cert-manager Certificate created on cycle 1

spec:
  katalog: ../katalog.yaml
  cr: ../crs/cert-valid.yaml
  cycles: 3

  expect:
    steady: true
    noErrors: true
    crds:
      platformResource:
        ops:
          - cycle: 1
            verb: create
            resource: certificates
            name: api-tls-cert
