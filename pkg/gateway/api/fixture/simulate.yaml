apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: gateway-serve-fixture-sim
  description: >
    Gateway Serve fixture — aggregates all three workload type simulates.
    Run individually via simulate/<workload>.yaml for focused iteration.

imports:
  - ./simulate/app.yaml
  - ./simulate/cert.yaml
  - ./simulate/monitoring.yaml
