apiVersion: orkestra.orkspace.io/v1
kind: Motif
metadata:
  name: fixture-namespaced
  description: >
    Fixture motif — Namespace + namespace-scoped policies, all forEach over spec.namespaces.

resources:
  namespaces:
    - name: "{{ .ns }}"
      forEach:
        field: spec.namespaces
        as: ns

  networkPolicies:
    - name: "{{ .metadata.name }}-{{ .ns }}-deny-all"
      namespace: "{{ .ns }}"
      forEach:
        field: spec.namespaces
        as: ns
      profile: deny-all

  resourceQuotas:
    - name: "{{ .metadata.name }}-{{ .ns }}-quota"
      namespace: "{{ .ns }}"
      forEach:
        field: spec.namespaces
        as: ns
      profile: small

  limitRanges:
    - name: "{{ .metadata.name }}-{{ .ns }}-limits"
      namespace: "{{ .ns }}"
      forEach:
        field: spec.namespaces
        as: ns
