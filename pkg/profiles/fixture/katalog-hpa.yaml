apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: profile-probe-hpa
  author: orkspace
  version: 0.1.0
  description: >
    ProfileProbe — living integration test for HPA behavior profiles.
    One CRD deploys five Deployments and five HPAs, each using a different
    behavior profile. The profile expands at katalog load time into a full
    HorizontalPodAutoscalerBehavior block. Run the operator and generate
    load to observe scale-up and scale-down behavior per profile.

spec:
  crds:
    profile-probe-hpa:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml

      operatorBox:
        default: true

        onCreate:
          deployments:
            - name: "{{ .metadata.name }}-web"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              reconcile: true

            - name: "{{ .metadata.name }}-api"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              reconcile: true

            - name: "{{ .metadata.name }}-latency"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              reconcile: true

            - name: "{{ .metadata.name }}-batch"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              reconcile: true

            - name: "{{ .metadata.name }}-cost"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              reconcile: true

          hpa:
            - name: "{{ .metadata.name }}-web-hpa"
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: "{{ .metadata.name }}-web"
              minReplicas: "1"
              maxReplicas: "10"
              behavior:
                profile: web

            - name: "{{ .metadata.name }}-api-hpa"
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: "{{ .metadata.name }}-api"
              minReplicas: "1"
              maxReplicas: "10"
              behavior:
                profile: api

            - name: "{{ .metadata.name }}-latency-hpa"
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: "{{ .metadata.name }}-latency"
              minReplicas: "2"
              maxReplicas: "20"
              behavior:
                profile: latency-sensitive

            - name: "{{ .metadata.name }}-batch-hpa"
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: "{{ .metadata.name }}-batch"
              minReplicas: "1"
              maxReplicas: "8"
              behavior:
                profile: batch

            - name: "{{ .metadata.name }}-cost-hpa"
              scaleTargetRef:
                apiVersion: apps/v1
                kind: Deployment
                name: "{{ .metadata.name }}-cost"
              minReplicas: "1"
              maxReplicas: "6"
              behavior:
                profile: cost-optimized

        status:
          fields:
            - path: phase
              value: "Pending"
              when:
                - field: status.phase
                  operator: notExists

            - path: phase
              value: "Ready"
              when:
                - field: "{{ allReplicasReady .children.deployment }}"
                  equals: "true"
