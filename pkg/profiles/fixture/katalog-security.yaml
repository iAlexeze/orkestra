apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: profile-probe-security
  author: orkspace
  version: 0.1.0
  description: >
    ProfileProbe — living integration test for security profiles.
    Creates one Deployment per security profile. Inspect each Deployment's
    container securityContext and pod securityContext to verify expansion.
    kubectl get deployment -l orkestra.io/managed-by=my-probe -o yaml

spec:
  crds:
    profile-probe-security:
      crdFile: ./crd.yaml
      crFiles:
        - ./cr.yaml

      workers: 1
      resync: 30s

      operatorBox:
        default: true

        onCreate:
          deployments:
            - name: "{{ .metadata.name }}-baseline"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              securityContext:
                profile: baseline
              podSecurity:
                profile: baseline
              reconcile: true

            - name: "{{ .metadata.name }}-restricted"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              securityContext:
                profile: restricted
              podSecurity:
                profile: restricted
              reconcile: true

            - name: "{{ .metadata.name }}-hardened"
              image: "{{ .spec.image }}"
              port: "{{ .spec.port }}"
              securityContext:
                profile: hardened
              podSecurity:
                profile: hardened
              reconcile: true

        status:
          fields:
            - path: phase
              value: "Pending"
              when:
                - field: status.phase
                  operator: notExists

            - path: phase
              value: "Ready"
              when:
                - field: "{{ allReplicasReady .children.deployments.my-probe-baseline }}"
                  equals: "true"
