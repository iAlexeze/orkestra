apiVersion: demo.orkestra.io/v1alpha1
kind: SaasBackend
metadata:
  name: my-saas
  namespace: default
spec:
  image: nginx:1.25
  region: us-east-1
  replicas: 2
  enableStorage: "true"
  enableEvents: "true"
  # domain: api.example.com          # uncomment for Route53
  # hostedZoneId: Z1234567890ABCDE   # uncomment for Route53