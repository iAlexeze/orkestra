apiVersion: demo.orkestra.io/v1alpha1
kind: ConfigMapDistribution
metadata:
  name: app-config-distribution
spec:
  configMapName: app-config
  sourceNamespace: platform
  targetNamespaces:
    - team-alpha
    - team-beta
