apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: beginner-sim
  description: >
    Suite for the beginner pack — runs simulate for all examples that have a simulate.yaml.

imports:
  - ./02-with-serviceaccount/simulate.yaml
