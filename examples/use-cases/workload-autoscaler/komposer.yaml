apiVersion: orkestra.orkspace.io/v1
kind: Komposer
metadata:
  name: workload-autoscaler-komposer
  description: >
    Composes all workload autoscaler katalogs into one runtime.

imports:
  files:
    - ./01-time-based/katalog.yaml
    - ./02-external-api/katalog.yaml
    - ./03-cross-operator/katalog.yaml
