apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: workload-autoscale-time-simulate
  description: >
    Validates the time-based workload autoscaler runs without errors.
    Replica count is time-dependent — simulation verifies structural correctness.

spec:
  katalog: ./katalog.yaml
  cr: ./cr.yaml
  cycles: 3

  expect:
    noErrors: true
