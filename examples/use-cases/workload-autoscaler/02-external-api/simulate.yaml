apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: workload-autoscale-external-simulate
  description: >
    Validates the external-API workload autoscaler runs without errors.
    Simulation exercises the reconcile loop with the external call mocked.

spec:
  katalog: ./katalog.yaml
  cr: ./cr.yaml
  cycles: 3

  expect:
    noErrors: true
