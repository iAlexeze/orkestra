apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: namespace-provisioner-suite
  description: >
    Suite for the namespace-provisioner use case — runs simulate for both
    examples. No cluster needed; all assertions run against the Orkestra
    simulation engine.

imports:
  - ./01-explicit/simulate.yaml
  - ./02-profiles/simulate.yaml
