apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: external-sim
  description: >
    Suite for the external: use-case examples — runs simulate for all examples
    that have a simulate.yaml. Each example stubs its external calls so no
    network or dev server is needed.

imports:
  - ./01-health-gate/simulate.yaml
  - ./02-config-inject/simulate.yaml
  - ./05-feature-flags/simulate.yaml
