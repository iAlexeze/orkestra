apiVersion: demo.orkestra.io/v1
kind: WebApp
metadata:
  name: my-app
spec:
  image: nginx:1.25
  # httpbin /json returns a static JSON object — a stand-in for a real config server.
  # Replace with your actual config service URL.
  serviceUrl: "https://httpbin.org"
  replicas: 1
