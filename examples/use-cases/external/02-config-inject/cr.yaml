apiVersion: demo.orkestra.io/v1
kind: WebApp
metadata:
  name: my-app
spec:
  image: nginx:1.25
  # Dev server responds to GET /config/my-app with a static JSON config blob.
  # Run `ork run --dev-server` before applying this CR.
  serviceUrl: "http://localhost:9999"
  replicas: 1
