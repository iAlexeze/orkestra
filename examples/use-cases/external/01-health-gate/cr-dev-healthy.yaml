apiVersion: demo.orkestra.io/v1
kind: WebApp
metadata:
  name: my-app-healthy
spec:
  image: nginx:1.25
  # Dev server /health returns 200 — Deployment is created.
  # Run `ork run --dev-server` before applying this CR.
  healthCheckUrl: "http://localhost:9999/health"
  replicas: 2
