apiVersion: demo.orkestra.io/v1
kind: Tenant
metadata:
  name: acme
  namespace: default
spec:
  name: "Acme Corp "           # normalize → "acme corp"
  environment: " Production "  # normalize → "production"
  domain: "https://acme.example.com/"  # normalize → "acme.example.com"
  tier: "PREMIUM"              # normalize → "premium"
