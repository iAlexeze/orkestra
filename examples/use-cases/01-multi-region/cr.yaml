# 01 — forEach: N deployments from a list
apiVersion: advanced.orkestra.io/v1alpha1
kind: MultiRegionApp
metadata:
  name: my-multi-region
  namespace: default
spec:
  image: nginx:1.25
  regions:
    - us-east-1
    - eu-west-1
    - ap-southeast-1
  defaultReplicas: 1

