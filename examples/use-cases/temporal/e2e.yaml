apiVersion: orkestra.orkspace.io/v1
kind: E2E
metadata:
  name: temporal-suite
  description: >
    Suite for the temporal track — business hours, maintenance window, regional peak.
    Runs all three examples in the same cluster.

imports:
  - ./01-business-hours/e2e.yaml
  - ./02-maintenance-window/e2e.yaml
  - ./03-regional-peak/e2e.yaml
  - ./04-autoscale/e2e.yaml
