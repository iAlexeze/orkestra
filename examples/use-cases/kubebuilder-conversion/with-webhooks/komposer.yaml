apiVersion: orkestra.orkspace.io/v1
kind: Komposer
metadata:
  name: cronjob-production
  description: >
    Production Komposer for the CronJob pattern.
    Inherits all operator behavior from katalog.yaml.
    Overrides runtime tuning for production workloads.

sources:
  files:
    - url: ./katalog.yaml

spec:
  crds:
    cronjob-v2:
      workers: 8
      resync: 15s

    cronjob-v1:
      workers: 4
      resync: 15s
