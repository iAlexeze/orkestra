apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: safe-reconcile-sim
  description: >
    Full suite — declarative CRDs (Monitor, Queue) reach steady state cleanly;
    typed CRD (App) panics and is recovered by safeReconcile.

imports:
  - ./simulate-monitor.yaml
  - ./simulate-queue.yaml
  - ./simulate-app.yaml
