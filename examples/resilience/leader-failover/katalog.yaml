apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: blockchain-operator
  version: 0.1.0
  description: BlockchainApp operator — demonstrates leader election resilience in a multi-replica Orkestra deployment.

spec:
  crds:
    blockchainapp:
      apiTypes:
        group: resilience.demo.orkestra.io
        version: v1alpha1
        kind: BlockchainApp
        plural: blockchainapps

      mutation:
        rules:
          - field: spec.replicas
            default: 1
            valueType: int
          - field: spec.port
            default: 8545
            valueType: int

      operatorBox:
        status:
          fields:
            - path: phase
              value: "Running"
            - path: nodeCount
              value: '{{ .spec.replicas }}'

        onCreate:
          statefulSets:
            - name: "{{ .metadata.name }}"
              image: "{{ .spec.image }}"
              replicas: "{{ .spec.replicas }}"
              reconcile: true
          services:
            - name: "{{ .metadata.name }}-rpc"
              port: "{{ .spec.port }}"
              targetPort: "{{ .spec.port }}"
              reconcile: true
