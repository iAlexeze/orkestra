expect:
  - name: Operator starts healthy
    after: cr-applied
    timeout: 60s
    kubectl:
      port-forward:
        - namespace: orkestra-system
          port: 8080
          path: /katalog/blockchainapp/health
          leaderElection:
            lease: orkestra-konductor
          jq: state
          equals: "healthy"
