# Invalid CR — denied by the domain required rule.
#   Reconcile time (no gateway): stored but reconcile halts, ValidationFailed condition written.
#   Apply time (gateway):        rejected before stored in etcd.
apiVersion: ecosystem.demo.orkestra.io/v1alpha1
kind: SecurityConfig
metadata:
  name: bad-cert
  namespace: default
spec:
  renewBefore: "720h"
  # spec.domain intentionally omitted — denied
