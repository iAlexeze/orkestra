# This App CR is missing spec.labels.team — admission should deny it.
apiVersion: ecosystem.demo.orkestra.io/v1alpha1
kind: App
metadata:
  name: bad-app
  namespace: default
spec:
  repo: "github.com/myorg/services"
  path: "apps/webapp"
  branch: "main"
  targetNamespace: "production"
  # spec.labels.team is intentionally missing
