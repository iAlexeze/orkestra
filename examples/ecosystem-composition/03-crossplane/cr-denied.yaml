# Invalid CR — will be denied by the team and region validation rules.
#   Apply time  (gateway webhook): denied before stored in etcd.
#   Reconcile time (no webhook):   stored but reconcile halts immediately.
apiVersion: ecosystem.demo.orkestra.io/v1alpha1
kind: Infra
metadata:
  name: rogue-db
  namespace: default
spec:
  type: postgres
  size: medium
  region: cn-north-1       # ← not in the approved region list — denied
  team: rogue              # ← not a registered platform team — denied
  approved: true
