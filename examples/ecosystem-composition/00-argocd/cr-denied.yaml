# Invalid CR — denied by the team ownership and namespace rules.
#   Apply time  (gateway webhook): rejected before stored in etcd.
#   Reconcile time (no webhook):   stored but reconcile halts immediately.
apiVersion: ecosystem.demo.orkestra.io/v1alpha1
kind: App
metadata:
  name: bad-app
  namespace: default
  # metadata.labels.team absent — ← ownership rule fires
spec:
  repo: "github.com/myorg/services"
  path: "apps/webapp"
  branch: "main"
  targetNamespace: "kube-system"   # ← not in the allowed namespace list — denied
  autoSync: true
