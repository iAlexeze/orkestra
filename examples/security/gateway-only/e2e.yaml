steps:
  - name: deploy-gateway
    action: deploy
    file: katalog.yaml
    flags:
      no-runtime: true

expect:
  - name: Protected namespace deletion blocked
    commands:
      - run: kubectl delete ns production
        exitCode: 1
        outputContains: "denied the request"
