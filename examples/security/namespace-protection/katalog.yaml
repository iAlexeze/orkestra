apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: namespace-protection
  author: orkspace
  version: 0.1.0
  description: >
    Demonstrates Orkestra namespace protection.
    A ValidatingWebhookConfiguration is registered that enforces namespace
    rules at admission time — CRs applied to restricted namespaces are
    rejected before they reach the reconciler.
    allowedNamespaces restricts App and Database to the "production" namespace.
    restrictedNamespaces blocks Cache from "kube-system" and "kube-public".

gateway:
  endpoint: http://orkestra-gateway:8080

security:
  namespaceProtection:
    enabled: true
    cleanupOnShutdown: true
    failurePolicy: Fail

spec:
  crds:
    app:
      apiTypes:
        group: security.orkestra.io
        version: v1alpha1
        kind: App
        plural: apps
      resync: 30s

      # Only allow App CRs in the "production" namespace.
      # Applying an App CR anywhere else is rejected at admission time.
      allowedNamespaces:
        - production

      operatorBox:

        status:
          fields:
            - path: phase
              value: "Running"
            - path: image
              value: "{{ .spec.image }}"
            - path: replicas
              value: "{{ .spec.replicas }}"

        onCreate:
          deployments:
            - name: "{{ .metadata.name }}-app"
              image: "{{ .spec.image }}"
              replicas: "{{ .spec.replicas }}"
              port: "{{ .spec.port }}"
              reconcile: true

          services:
            - name: "{{ .metadata.name }}-svc"
              port: "80"
              targetPort: "{{ .spec.port }}"
              reconcile: true

    database:
      apiTypes:
        group: security.orkestra.io
        version: v1alpha1
        kind: Database
        plural: databases
      resync: 30s

      # Only allow Database CRs in the "production" namespace.
      allowedNamespaces:
        - production

      operatorBox:

        status:
          fields:
            - path: phase
              value: "Ready"
            - path: engine
              value: "{{ .spec.engine }}"

        onCreate:
          configMaps:
            - name: "{{ .metadata.name }}-db-config"
              reconcile: true
              data:
                engine: "{{ .spec.engine }}"
                storageSize: "{{ .spec.storageSize }}"

    cache:
      apiTypes:
        group: security.orkestra.io
        version: v1alpha1
        kind: Cache
        plural: caches
      resync: 30s

      # Block Cache CRs from system namespaces.
      # Cache can be applied to any other namespace.
      restrictedNamespaces:
        - kube-system
        - kube-public

      operatorBox:

        status:
          fields:
            - path: phase
              value: "Ready"
            - path: maxMemory
              value: "{{ .spec.maxMemory }}"

        onCreate:
          configMaps:
            - name: "{{ .metadata.name }}-cache-config"
              reconcile: true
              data:
                maxMemory: "{{ .spec.maxMemory }}"
                evictionPolicy: "{{ .spec.evictionPolicy }}"
