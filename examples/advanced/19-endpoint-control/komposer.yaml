apiVersion: orkestra.orkspace.io/v1
kind: Komposer
metadata:
  name: endpoint-control
  description: >
    Composes all three endpoint-control katalogs — selective health disable,
    full endpoint disable, and fully dark (crossAccess + endpoints combined).

imports:
  files:
    - 01-selective-health/katalog.yaml
    - 02-full-disable/katalog.yaml
    - 03-fully-dark/katalog.yaml
