apiVersion: demo.orkestra.io/v1alpha1
kind: Database
metadata:
  name: my-db
  namespace: default
spec:
  engine: postgres
  version: "14"
  storage: 10Gi
  backup: true    # triggers the optional backup CronJob in the hook
