# Valid CR — passes all validation rules
# Note: replicas and port not declared — mutation will apply defaults
apiVersion: demo.orkestra.io/v1alpha1
kind: Website
metadata:
  name: my-site
  namespace: default
spec:
  image: myorg/nginx:1.25
  environment: production
  replicas: 2
  # replicas: not declared → mutation defaults to "2"
  # port: not declared → mutation defaults to "8080"
