# Invalid CR — will be denied by the image prefix rule
# With ENABLE_WEBHOOKS=true: denied at kubectl apply time
# Without webhooks: stored but reconcile halts with validation error
apiVersion: demo.orkestra.io/v1alpha1
kind: Website
metadata:
  name: bad-site
  namespace: default
spec:
  image: docker.io/nginx:1.25   # ← NOT from myorg/ — will be denied
  replicas: 2
  port: "8080"
