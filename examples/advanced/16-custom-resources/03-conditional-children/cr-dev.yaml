apiVersion: platform.example.io/v1alpha1
kind: AppEnvironment
metadata:
  name: dev-env
  namespace: default
spec:
  name: "Development Environment"
  tier: dev
  cache:
    enabled: "false"
  search:
    enabled: "false"
    indexName: "dev-index"
    replicas: 1
