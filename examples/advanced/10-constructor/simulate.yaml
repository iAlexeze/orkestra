apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: pipeline-constructor-sim
  description: Verify typed constructor fires — first pipeline step Job created in cycle 1

spec:
  katalog: ./katalog.yaml
  cr: ./cr.yaml
  cycles: 5

  expect:
    steady: true
    noErrors: true
    ops:
      - cycle: 1
        verb: create
        resource: jobs
        name: build-and-test-build
