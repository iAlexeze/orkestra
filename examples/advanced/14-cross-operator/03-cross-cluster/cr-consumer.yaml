apiVersion: cross.orkestra.io/v1alpha1
kind: Consumer
metadata:
  name: my-producer   # same name — the cross block finds Producer by matching name
  namespace: default
spec:
  image: nginx:stable-alpine
  replicas: 1
