# Schema reference: https://orkestra.sh/docs/reference/schema/katalog/
apiVersion: orkestra.orkspace.io/v1
kind: Katalog
metadata:
  name: webapp-hooks-only
  author: myorg
  version: 1.0.0
  description: >
    The WebApp operator in hooks-only mode. The hook owns both resources:
    Deployment and Service. No declared templates.
    Use when every resource requires computed values or type-safe control
    that templates cannot express.
  tags:
    - migration
    - hooks
    - typed

spec:
  crds:
    hooksApp:
      apiTypes:
        group: allopt.demo.orkestra.io
        version: v1alpha1
        kind: HooksApp
        plural: hooksapps
        object: HooksApp
        objectList: HooksAppList
        location: github.com/orkspace/from-controller-runtime-all-options/options/hooks/api/v1alpha1
        alias: hooksv1alpha1

      allowedNamespaces:
        - default

      operatorBox:

        # No declared templates. The hook manages both resources.
        reconciler:
          default: true   # GenericReconciler still runs — hook is additive
          hooks:
            location: github.com/orkspace/from-controller-runtime-all-options/options/hooks/hooks
            function: WebAppHooks
            alias: hookswh
            managedResources:
              - kind: Deployment
              - kind: Service

        status:
          fields:
            - path: phase
              value: "Running"
            - path: endpoint
              value: "{{ .metadata.name }}-svc.{{ .metadata.namespace }}.svc.cluster.local"
            - path: replicas
              value: "{{ .spec.replicas }}"
