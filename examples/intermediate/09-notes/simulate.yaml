apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: notes-suite-sim
  description: >
    Notes suite — built-in notes and user-defined notes. No cluster required.

imports:
  - ./01-built-in/simulate.yaml
  - ./02-user-defined/simulate.yaml
  - ./03-motifs/simulate.yaml
