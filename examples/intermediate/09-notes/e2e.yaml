# https://orkestra.sh/docs/reference/schema/e2e/
apiVersion: orkestra.orkspace.io/v1
kind: E2E
metadata:
  name: notes-suite
  description: >
    Suite for the notes track — built-in notes, user-defined notes.
    Runs both examples in the same cluster.

imports:
  - ./01-built-in/e2e.yaml
  - ./02-user-defined/e2e.yaml
  - ./03-motifs/e2e.yaml
