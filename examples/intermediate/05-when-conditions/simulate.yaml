# https://orkestra.sh/docs/reference/cli/simulate/
apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: when-conditions-sim
  description: >
    Aggregates simulate suites for both when-conditions sub-examples.
    conditional-reconciliation runs with --envtest.

imports:
  - ./conditional-resources/simulate.yaml
  - ./conditional-reconciliation/simulate.yaml
