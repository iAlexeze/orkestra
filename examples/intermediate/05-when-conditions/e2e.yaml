# https://orkestra.sh/docs/reference/schema/e2e/
apiVersion: orkestra.orkspace.io/v1
kind: E2E
metadata:
  name: when-conditions-e2e
  description: >
    Aggregates both when-conditions sub-examples: resource-level conditions
    (conditional-resources) and pre-reconcile gates (conditional-reconciliation).

imports:
  - ./conditional-resources/e2e.yaml
  - ./conditional-reconciliation/e2e.yaml
