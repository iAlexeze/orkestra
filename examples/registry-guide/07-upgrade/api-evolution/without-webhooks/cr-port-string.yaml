# Old format — flat spec.port (deprecated but still accepted)
# normalize: spec.expose.port = coalesce(nil, "9999") = "9999"
# The operator reconciles this identically to cr-port-structured.yaml
apiVersion: rkguide.demo/v1
kind: WebApp
metadata:
  name: webapp-flat
  namespace: default
spec:
  image: ghcr.io/orkspace/orkestra-dev-server:0.7.5
  port: 9999
  replicas: 1
