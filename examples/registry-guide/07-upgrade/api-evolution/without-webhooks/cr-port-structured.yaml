# New format — structured spec.expose (preferred)
# normalize: spec.expose.port = coalesce(9999, nil) = 9999
# Identical reconcile behavior to cr-port-string.yaml
apiVersion: rkguide.demo/v1
kind: WebApp
metadata:
  name: webapp-structured
  namespace: default
spec:
  image: ghcr.io/orkspace/orkestra-dev-server:0.7.5
  replicas: 1
  expose:
    port: 9999
    host: ""
