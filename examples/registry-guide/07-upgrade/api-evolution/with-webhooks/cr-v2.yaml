# v2 CR — structured expose block (current format)
# Stored directly — no conversion needed.
apiVersion: rkguide.demo/v2
kind: WebApp
metadata:
  name: my-webapp-v2
  namespace: default
spec:
  image: ghcr.io/orkspace/orkestra-dev-server:0.7.5
  replicas: 1
  expose:
    port: 9999
    host: ""
    protocol: HTTP
