apiVersion: rkguide.demo/v1
kind: PlatformApp
metadata:
  name: my-platform-app-denied
  namespace: default
spec:
  # Image from a non-allowed registry — validation rule will deny this CR
  image: docker.io/nginx:1.25
  port: 8080
  replicas: 2