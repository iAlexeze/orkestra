apiVersion: orkestra.orkspace.io/v1
kind: Simulate
metadata:
  name: typed-komposer-sim
  description: >
    Suite for the typed Komposer — runs simulate for all three operators:
    webapp, cache, and database (typed hooks).

imports:
  - ../02-katalog-api/simulate.yaml
  - ../03-katalog-cache/simulate.yaml
  - ../10-hooks-katalog/simulate.yaml
