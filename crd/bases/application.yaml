---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applications.platform.ialexeze.io
spec:
  group: platform.ialexeze.io
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    shortNames:
    - app
    singular: application
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.sync.status
      name: Sync
      type: string
    - jsonPath: .status.sync.revision
      name: Revision
      type: string
    - jsonPath: .status.health.status
      name: Health
      type: string
    name: "v1alpha1"    
    schema:
      openAPIV3Schema:
        description: Application is a definition of an Application resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              destination:
                description: Destination is the target cluster/namespace
                properties:
                  name:
                    description: Name is the cluster name (alternative to Server)
                    type: string
                  namespace:
                    description: Namespace is the target namespace
                    type: string
                  server:
                    description: Server is the API server URL of the target cluster
                    type: string
                required:
                - namespace
                - server
                type: object
              project:
                description: Project is the ArgoCD project this app belongs to
                type: string
              source:
                description: Source is the Git repository source
                properties:
                  helm:
                    description: Helm specific configuration
                    properties:
                      parameters:
                        items:
                          description: HelmParameter represents a Helm parameter
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                      releaseName:
                        type: string
                      skipCrds:
                        type: boolean
                      valueFiles:
                        items:
                          type: string
                        type: array
                      values:
                        type: string
                      version:
                        type: string
                    type: object
                  kustomize:
                    description: Kustomize specific configuration
                    properties:
                      images:
                        items:
                          type: string
                        type: array
                      namePrefix:
                        type: string
                      nameSuffix:
                        type: string
                      version:
                        type: string
                    type: object
                  path:
                    description: Path is the directory path within the repository
                    type: string
                  repoURL:
                    description: RepoURL is the Git repository URL
                    type: string
                  targetRevision:
                    description: TargetRevision is the Git revision (branch/tag/commit)
                    type: string
                required:
                - path
                - repoURL
                - targetRevision
                type: object
              syncPolicy:
                description: SyncPolicy controls sync behavior
                properties:
                  automated:
                    description: Automated sync options
                    properties:
                      prune:
                        type: boolean
                      selfHeal:
                        type: boolean
                    type: object
                  retry:
                    description: Retry controls failed sync retry behavior
                    properties:
                      backoff:
                        description: Backoff controls retry backoff
                        properties:
                          duration:
                            type: string
                          factor:
                            type: integer
                          maxDuration:
                            type: string
                        type: object
                      limit:
                        type: integer
                    type: object
                  syncOptions:
                    description: Sync options (e.g., "Validate=false", "Prune=false")
                    items:
                      type: string
                    type: array
                type: object
            required:
            - destination
            - source
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application
            properties:
              conditions:
                description: Conditions represent observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              health:
                description: Health status
                properties:
                  message:
                    type: string
                  status:
                    type: string
                required:
                - status
                type: object
              history:
                description: History of sync operations
                items:
                  description: SyncHistory represents a historical sync operation
                  properties:
                    deployStartedAt:
                      format: date-time
                      type: string
                    deployedAt:
                      format: date-time
                      type: string
                    revision:
                      type: string
                    revisions:
                      items:
                        type: string
                      type: array
                    source:
                      description: ApplicationSource defines the source configuration
                      properties:
                        helm:
                          description: Helm specific configuration
                          properties:
                            parameters:
                              items:
                                description: HelmParameter represents a Helm parameter
                                properties:
                                  name:
                                    type: string
                                  value:
                                    type: string
                                required:
                                - name
                                - value
                                type: object
                              type: array
                            releaseName:
                              type: string
                            skipCrds:
                              type: boolean
                            valueFiles:
                              items:
                                type: string
                              type: array
                            values:
                              type: string
                            version:
                              type: string
                          type: object
                        kustomize:
                          description: Kustomize specific configuration
                          properties:
                            images:
                              items:
                                type: string
                              type: array
                            namePrefix:
                              type: string
                            nameSuffix:
                              type: string
                            version:
                              type: string
                          type: object
                        path:
                          description: Path is the directory path within the repository
                          type: string
                        repoURL:
                          description: RepoURL is the Git repository URL
                          type: string
                        targetRevision:
                          description: TargetRevision is the Git revision (branch/tag/commit)
                          type: string
                      required:
                      - path
                      - repoURL
                      - targetRevision
                      type: object
                  required:
                  - deployedAt
                  - revision
                  - source
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase
                type: string
              sync:
                description: Sync status summary
                properties:
                  revision:
                    type: string
                  revisions:
                    items:
                      type: string
                    type: array
                  status:
                    type: string
                required:
                - revision
                - status
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
